NEXT_PUBLIC_NEON_AUTH_URL=""# Get the auth url from App Backend -> Auth -> COnfiguration section

# Leave unset in local dev — Vite proxies /api to the backend on port 3000
# VITE_API_BASE_URL="http://localhost:3000"